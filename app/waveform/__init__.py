"""(Phase 5) Waveform data engine: model, decimation, chunk cache, viewport.

Kept separate from the GUI so the rendering implementation can be upgraded
later without touching the engine (spec §23).
"""
